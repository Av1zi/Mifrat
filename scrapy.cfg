[settings]
default = scraper.settings

# No [deploy] section — Zyte Scrapy Cloud was abandoned (see DECISIONS.md).
# Spiders run via `scraper/run_spider.py <spider_name>`, invoked either by
# a GitHub Actions job (cloud vendors) or the Jetson Nano (TMS). Run
# `scrapy crawl <name>` directly from the repo root for local testing.
