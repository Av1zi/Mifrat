# Copy this file to .env and fill in real values for local testing.
# .env itself is gitignored — never commit real credentials.
#
# These are the same two secrets that live in GitHub Actions secrets for
# the sync-and-deploy workflow (Settings -> Secrets and variables -> Actions
# in the GitHub repo) — this file is only for running
# scraper/sync_from_scrapy_cloud.py locally.

SHUB_APIKEY=
SHUB_PROJECT_ID=
